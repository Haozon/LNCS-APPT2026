# JSQKV：用于对照英文稿的中文稿

说明：本文件对应当前版本的 [samplepaper.tex](D:/Git/LNCS-APPT2026/samplepaper.tex)，主要用于中英对照和表达比较。结构、论证顺序、表格数据与英文稿保持一致，但这里使用中文行文，不考虑中文 LaTeX 编译。

## 标题

**JSQKV：面向 KV Cache 压缩与解码加速的联合稀疏化与量化框架**

## 摘要

随着长上下文大语言模型在文本生成、问答和交互式应用中的广泛部署，自回归解码阶段越来越受到 KV Cache 存储占用与内存带宽的限制，而不再主要受原始算术吞吐约束。现有 KV Cache 优化方法通常从稀疏化或低比特量化单独出发，但前者在高压缩比下容易带来明显精度退化，后者又缺乏对历史词元重要性的直接建模；更重要的是，将二者简单顺序叠加，往往难以稳定转化为高效的在线解码收益。为此，本文提出 JSQKV，一种面向长上下文解码的 KV Cache 联合稀疏–量化压缩框架，从压缩策略、数值表示与运行时执行三个层面进行协同设计。具体而言，JSQKV 基于注意力分数构造差分稀疏策略，在词元粒度上分配不同稀疏预算；进一步引入面向在线解码的双窗口执行机制，以及基于 Hadamard 变换的 Per-Token-Tile 量化方法，以缓解 Key 中异常值带来的低比特量化不稳定性；同时设计 Bitmap-Based Sparse-Quant 数据格式与相应解码算子，使压缩后的 KV Cache 能够以压缩状态驻留内存，并高效支持在线解码计算。实验结果表明，在 Meta-Llama-3-8B 上，JSQKV 在相同压缩预算下持续优于顺序式 MUSTAFAR+KIVI 基线；在输入长度 4096、输出长度 256 的长上下文设置下，采用 70% KV 稀疏率与 2-bit 量化配置时，JSQKV 在 batch size 4 下可将端到端吞吐较稠密基线提升约 44.2%。同时，2-bit 量化路径可将 KV Cache 压缩率进一步提高到 3.48×。这些结果表明，高效的 KV Cache 压缩并不只是提高压缩率本身，而是需要对压缩策略、量化表示和解码执行路径进行联合优化。

长上下文自回归解码越来越受限于 KV Cache 的容量与内存带宽，而不再主要受限于原始算术吞吐。现有 KV Cache 压缩方法通常采用剪枝或低比特量化，但激进压缩往往会带来明显的精度退化，而将稀疏与量化简单串联也未必能够带来高效的解码执行。为此，本文提出 \`JSQKV\`，一种面向 KV Cache 压缩与解码加速的联合框架。JSQKV 协同设计了四个组件：基于注意力的差分稀疏策略、用于解码阶段在线压缩的双窗口机制、基于 Hadamard 变换的 token-tile 量化，以及带有 Tensor Core 友好解码路径的 bitmap-based sparse-quant 存储布局。上述组件共同作用，在保留更关键上下文词元的同时稳定低比特 KV 表示，并使缓存数据在内存中保持压缩状态，仅在高效计算所需时重构为稠密 tile。实验结果表明，在 LongBench 上，JSQKV 在相同压缩预算下持续优于顺序式 MUSTAFAR+KIVI 基线；在 Meta-Llama-3-8B 上，JSQKV 还能在中小 batch 场景下提升端到端吞吐，并显著降低 KV Cache 大小。这些结果表明，
想要实现高效的 KV Cache 压缩，需要对压缩策略、数值表示与运行时执行进行联合优化。

## 1 引言

随着大语言模型的上下文窗口不断增长，自回归解码越来越受到 KV Cache 占用和内存带宽的制约。KV Cache 的内存开销随上下文长度线性增长，而解码阶段并行性有限，又需要反复访问 KV Cache，因此比预填充阶段更容易受内存带宽限制。在长上下文场景下，这一瓶颈进一步限制了吞吐、batch size 和部署效率。

已有 KV Cache 优化工作主要沿着两个方向展开。一类方法通过词元驱逐或稀疏化减少保留的历史信息。StreamingLLM、H\$_2\$O、SnapKV 和 PyramidKV 只保留部分历史词元，而 ThinK、KVPruner 和 MUSTAFAR 则进一步探索了按通道、结构化和非结构化的稀疏方式。这些研究表明，历史词元的重要性并不相同，基于重要性的缓存缩减能够有效缓解内存和带宽压力。另一类方法通过低比特量化压缩 KV Cache 元素的数值表示。KVQuant、QuaRot 和 KIVI 分别探索了超低比特 KV 表示、分布整形以及对 Key 和 Value 的非对称量化。这些方法能够直接降低 KV Cache 占用和带宽成本，但关注点主要在于如何量化每个缓存元素，而不是保留哪些历史信息。

总体来看，已有工作说明稀疏化和量化都有效，但两者大多仍被分开研究。实际中的联合压缩并不是简单的顺序叠加：一旦稀疏化改变了保留词元和缓存访问模式，合适的量化粒度、压缩数据布局以及解码时 kernel 设计也都会随之改变。若缺少这种协同设计，算法层面的压缩收益就未必能稳定转化为实际的端到端加速。

为此，本文提出 \`JSQKV\`，一个面向 KV Cache 压缩与解码加速的联合稀疏-量化框架。JSQKV 共同设计压缩策略、数值表示和在线执行机制。其核心思想很直接：根据词元对未来注意力的重要性分配不同的压缩预算；对剩余 KV 状态采用与词元对齐、数值更稳定的低比特表示；并尽可能让 KV Cache 以压缩形态驻留内存，直到解码 kernel 需要稠密 tile 时再进行重构。通过这种联合设计，JSQKV 在保持 KV Cache 紧凑存储的同时，更有效地减少内存流量并提升端到端吞吐。

本文的主要贡献如下：

- **压缩策略。** 提出基于注意力分数的差分稀疏策略，在词元粒度上为不同历史词元分配不同的稀疏级别，而不是使用单一的全局压缩率。
- **在线执行。** 设计面向自回归解码的双窗口在线执行机制，将差分稀疏从预填充阶段扩展到在线解码阶段。
- **数值表示。** 提出基于 Hadamard 变换的 Per-Token-Tile 量化方法，用于缓解 Key 中异常值导致的低比特量化不稳定性，并使量化粒度更好地与词元级压缩决策对齐。
- **系统协同。** 设计 Bitmap-Based Sparse-Quant 数据格式及其对应解码 kernel，使压缩后的 KV Cache 能够在内存中保持紧凑，并将压缩有效转化为更低内存流量和端到端解码加速。

实验结果表明，在高压缩比下，JSQKV 能够在模型效果和系统效率之间取得较好的平衡。在 Meta-Llama-3-8B 上，采用 70\% KV Cache 稀疏率和 2-bit 量化时，JSQKV 在长上下文解码、batch size 为 4 的设置下，相比稠密基线可将端到端吞吐提升约 44.2\%。这些结果说明，高效的 KV Cache 压缩需要在压缩策略、数值表示和在线执行三个层面进行联合优化。

## 2 方法

### 2.1 方法概览

JSQKV 按照自回归推理的时间顺序进行设计。在预填充阶段，模型能够访问完整的 prompt 注意力，因此可以估计哪些历史词元在后续生成中更可能保持重要。基于这些分数，每个词元会被分配到三个压缩等级之一：完整保留、部分保留特征、或直接移除。在解码阶段，系统维护在线注意力统计，使新生成词元也能够在积累足够观测后应用同样的压缩策略。在得到稀疏决策后，剩余非零值使用低比特 token-tile 量化，并存储为适配解码执行的 bitmap-based sparse-quant 格式。

### 2.2 基于注意力分数的差分稀疏

JSQKV 的出发点是：不同历史词元对未来解码的贡献并不相同。被后续 query 反复关注的词元更可能携带持久上下文信息，而很少被关注的词元则可以被更激进地压缩。受到 SnapKV 观察窗口分析的启发，本文用 prompt 末尾 \(L_{\text{obs}}\) 个词元的注意力分布来估计词元重要性。

设预填充阶段的因果注意力矩阵为 \(\mathbf{A} \in \mathbb{R}^{T_q \times T_k}\)。对于前缀中的第 \(j\) 个词元（\(j \leq T_k - L_{\text{obs}}\)），其重要性分数定义为：

```tex
s_j = \frac{T_k}{L_{\text{obs}}} \sum_{i=T_k-L_{\text{obs}}+1}^{T_k} A_{i,j}.
```

观察窗口中的词元默认视为高重要性并优先保留。归一化因子 \(T_k/L_{\text{obs}}\) 用于让不同长度序列上的分数具有更好的可比性。

在得到分数 \(\{s_j\}\) 后，JSQKV 按分位数将词元划分为三个等级。设 \([p_0,p_1,p_2]\) 为 Level 0、Level 1、Level 2 的目标占比，满足 \(p_0+p_1+p_2=1\)。对应阈值为：

```tex
\tau_{\text{high}} = \operatorname{Percentile}(s, 1-p_0), \\
\tau_{\text{low}}  = \operatorname{Percentile}(s, p_2).
```

然后按如下规则分配等级：

```tex
\operatorname{level}(j) =
\begin{cases}
0, & s_j \geq \tau_{\text{high}}, \\
1, & \tau_{\text{low}} \leq s_j < \tau_{\text{high}}, \\
2, & s_j < \tau_{\text{low}}.
\end{cases}
```

Level 0 词元完整保留，Level 2 词元直接移除。对于 Level 1 词元，JSQKV 在 key 与 value 上分别执行特征级 TopK 稀疏，仅保留幅值最大的维度：

```tex
\tilde{k}_{j,i} =
\begin{cases}
k_{j,i}, & i \in \operatorname{TopK}(|\mathbf{k}_j|,\lfloor (1-\rho_1)d \rfloor), \\
0, & \text{otherwise}.
\end{cases}
```

这一设计能够在高平均压缩比下，更仔细地保留关键上下文词元。

### 2.3 受预算约束的任务自适应配置

固定的平均稀疏预算并不能唯一确定一种差分稀疏策略。即使目标压缩比保持不变，不同的“完整保留 / 部分稀疏 / 直接移除”预算分配方式，也可能在不同任务上带来明显不同的精度结果。有些任务更依赖较大比例的完整保留词元，而另一些任务则能够容忍更激进的词元移除，只要少量承载证据的关键 token 被保留下来即可。因此，在 JSQKV 中，差分稀疏策略并不是通过一个对所有任务都固定不变的三层配置来实例化，而是在显式预算约束下进行配置。

我们将三层稀疏策略参数化为：

```tex
c = ([p_0,p_1,p_2], \rho_1),
```

其中 \(p_0\)、\(p_1\) 和 \(p_2\) 分别表示 Level 0、Level 1 和 Level 2 词元的占比，并满足：

```tex
p_0 + p_1 + p_2 = 1.
```

其中，Level 0 词元保持稠密，Level 1 词元采用特征级稀疏率 \(\rho_1\)，Level 2 词元则被完全移除。令 \(\rho_0 = 0\)、\(\rho_2 = 1\)，则平均稀疏预算 \(B\) 需要满足：

```tex
B = p_0 \rho_0 + p_1 \rho_1 + p_2 \rho_2 = p_1 \rho_1 + p_2.
```

这一定义说明：在相同总体预算下，往往会存在多组可行策略。因此，严格的同预算比较不仅需要固定 \(B\)，还需要明确预算究竟如何分配到“稠密保留 / 部分稀疏 / 直接移除”三类词元上。

为了在目标预算下实例化 JSQKV，我们采用一个轻量的校准式配置求解过程。与穷举所有配置不同，我们只在较小的可行网格上搜索 \((p_0,\rho_1)\) 组合，并通过解析方式恢复剩余比例：

```tex
p_1 = \frac{1 - p_0 - B}{1 - \rho_1},
\qquad
p_2 = 1 - p_0 - p_1.
```

不满足 \(p_0,p_1,p_2 \in [0,1]\) 的候选会被直接丢弃。这样便得到了一组在相同平均压缩预算下、但对应不同“完整保留 / 部分保留 / 直接移除”权衡方式的候选策略。

随后，我们在一个小规模校准集上选择最终配置：

```tex
c^\star = \arg\max_{c \in \mathcal{C}(B)} \mathrm{Metric}(D_{\mathrm{cal}}; c),
```

其中 \(\mathcal{C}(B)\) 表示预算 \(B\) 下的可行候选集，\(D_{\mathrm{cal}}\) 表示保留出的校准子集。为降低过拟合风险，得到的最优配置还会在一个与校准集不重叠的验证子集上再次评估；若校准增益无法泛化，则退回更简单的同预算统一稀疏配置。这个过程有意保持轻量：它既保证了同预算比较的公平性，又允许 JSQKV 根据不同任务的敏感性，自动调整“稠密 / 部分 / 移除”的预算分配。

从整体框架看，这一预算约束下的配置机制具有重要实践意义。它把前文的词元重要性建模与后续的 sparse-quant 运行时连接起来：重要性估计器负责给词元排序，预算求解器负责决定各压缩等级应当分到多少预算，而 sparse-quant 表示则负责落实最终策略。也就是说，这个配置求解器并不是一个独立的压缩原语，而是把固定全局预算转化为任务相关差分策略的关键环节。

### 2.4 解码阶段的双窗口在线机制

上述预填充策略不能直接用于自回归解码，因为某个词元刚写入 KV Cache 时，它未来会被多少注意力访问仍然未知。为了解决这个问题，JSQKV 引入双窗口在线机制。

系统维护两个相邻窗口，大小均为 \(W\)。Window A 保存“等待被压缩”的词元，Window B 保存“较新的词元”，它们持续为 Window A 提供新的注意力观测。当产生新的 query \(\mathbf{q}_t\) 时，其对 Window A 的注意力为：

```tex
\boldsymbol{\alpha}_t =
\operatorname{softmax}\!\left(\frac{\mathbf{q}_t \mathbf{K}_A^\top}{\sqrt{d}}\right).
```

这些分数会累加到一个 attention buffer 中。当 Window B 填满时，Window A 中不同位置的词元会有不同的观测次数，因此需要做归一化。对 Window A 中位置为 \(i\) 的词元，其观测次数为：

```tex
N^{(i)} = 2W - 1 - i,
```

于是归一化分数为：

```tex
\bar{\alpha}^{(i)} = \frac{\mathrm{Acc}[:,:,i]}{2W - 1 - i}.
```

解码器随后复用预填充阶段得到的阈值 \((\tau_{\text{high}}, \tau_{\text{low}})\)，对 Window A 中的词元进行分级、压缩，并将 Window B 滑动为新的 Window A。通过这种方式，每个词元在被压缩前都能获得一定数量的后续注意力观测，同时未压缩词元的总量始终被限制在 \(O(W)\)。

### 2.5 基于 Hadamard 的 token-tile 量化

词元级的稀疏决策意味着量化也应当与词元对齐。如果多个词元共享一个较粗粒度的量化尺度，那么重要词元与非重要词元的数值分布就可能彼此干扰。为避免这种失配，JSQKV 使用 *per-token-tile* 量化。

对某个词元向量 \(\mathbf{x}_t \in \mathbb{R}^d\)（可以是 key 或 value），沿特征维划分为 \(M=d/g\) 个连续 tile：

```tex
\mathbf{x}_t = [\mathbf{x}_{t,1},\mathbf{x}_{t,2},\ldots,\mathbf{x}_{t,M}],
\qquad
\mathbf{x}_{t,m} \in \mathbb{R}^{g}.
```

对于每个 tile，计算局部 scale \(s_{t,m}\) 与 zero point \(z_{t,m}\)，再进行 \(b\)-bit 量化：

```tex
\mathbf{q}_{t,m} =
\operatorname{clip}\!\left(
\operatorname{round}\!\left(\frac{\mathbf{x}_{t,m}}{s_{t,m}} + z_{t,m}\right),
0,\,
2^b-1
\right).
```

更细粒度的 tile 能够缩小局部动态范围，也能更好匹配词元级的压缩决策。

低比特 key 量化对异常值尤其敏感。为提高稳定性，JSQKV 在 key 上引入 Hadamard 变换，并在解码时对 query 应用同样变换。这一做法与 QuaRot 的思想一致。设 \(\mathbf{H}\) 为 Hadamard 矩阵，则：

```tex
\tilde{\mathbf{q}}_i = \frac{1}{\sqrt{d}}\mathbf{H}\mathbf{q}_i, \qquad
\tilde{\mathbf{k}}_j = \frac{1}{\sqrt{d}}\mathbf{H}\mathbf{k}_j,
```

并满足：

```tex
\tilde{\mathbf{q}}_i^\top \tilde{\mathbf{k}}_j = \mathbf{q}_i^\top \mathbf{k}_j.
```

因此，注意力得分保持不变，而激活分布会更适合低比特量化。

图注对应：

- **图 1.** LLaMA2-7B 样本中，Hadamard 变换前后的 key 激活分布对比。该变换可以削弱长尾分布，同时保留足够的幅值差异以支持后续特征级稀疏。

### 2.6 Sparse-Quant Attention Kernel

仅有压缩精度还不够，数值表示还必须能够高效映射到解码算子。为此，JSQKV 对每个 \(1 \times 64\) tile 存储五个组成部分：编码非零位置的 bitmap、打包后的低比特数值、指向 packed-value 数组的 tile 偏移、以及每个 tile 对应的 scale 和 zero point。图 2 展示了这种布局。该设计在保持随机访问能力的同时，使压缩表示足够规则，便于 GPU 高效访问。

图注对应：

- **图 2.** Bitmap-based sparse-quant 布局。偏移、位图、打包后的低比特数值以及每个 tile 的元数据分别存储在独立数组中，从而在保证紧凑性的同时维持规则的 GPU 访问模式。

在解码阶段，JSQKV 采用“加载时保持压缩、计算时重构为稠密”的策略。图 3 展示了整个解码流程：算子首先从内存中加载 bitmap 和 packed value，再执行 bit extraction 与反量化，将非零值 scatter 到稠密 shared-memory tile 中，最后在 Tensor Core 上执行矩阵-向量乘法。这样做避免了传统稀疏算子常见的不规则访存与控制流分岔，同时仍然保留了低比特稀疏表示带来的带宽优势。

图注对应：

- **图 3.** Bitmap-based sparse-quant 解码流程。压缩 tile 被加载、解包、反量化并 scatter 到 shared memory 中的稠密 tile，随后送入 Tensor Core 进行矩阵-向量计算。

## 3 实验

### 3.1 实验设置

为评估 JSQKV，除特别说明外，所有实验均在同一套硬件平台上完成。系统使用 NVIDIA A100 80GB PCIe GPU、Intel Xeon CPU 和 252GB 主存。实现基于 Python 3.10、PyTorch 2.6.0、CUDA 12.4、HuggingFace Transformers 与 FlashAttention。对压缩和解码关键路径，JSQKV 使用自定义 CUDA/Triton kernel 进行 sparse-quant 打包与解码重构。

主评测模型为 Meta-Llama-3-8B，同时在 Llama2-7B、Llama2-13B、Mistral-7B 和 Qwen2.5-7B 上进行跨模型验证。精度评估采用 LongBench 官方评测流程。输入统一截断到 8192 token；若样本超过该长度，则保留前 4096 token 与后 4096 token。所有实验均不进行额外训练或微调。

**预算实例化协议。**
对于所有差分稀疏与联合稀疏-量化设置，我们都不会预先固定一个对所有任务通用的三层稀疏配置。相反，在每个目标平均稀疏预算下，我们都使用第 2.3 节所述的校准式配置过程来实例化策略。具体而言，对每个目标预算 \(B \in \{0.5, 0.7\}\)，我们构造满足公式 \(\eqref{eq:budget_constraint}\) 的三层配置候选集 \(c = ([p_0,p_1,p_2], \rho_1)\)，在一个小规模保留校准集上评估这些候选，并在相同预算下选择表现最好的配置。随后，在正式运行完整 LongBench 评测之前，我们还会在一个与校准集不重叠的验证集上再次评估所选配置。

除特别说明外，这一校准协议会固定其余选择超参数，只搜索预算分配本身，也就是 dense / partial / evict 三类词元的占比以及中间层的稀疏率。该协议保证了同预算比较在两个意义上是公平的：一方面，被比较方法具有相同的目标平均稀疏率；另一方面，差分策略是通过统一的校准流程得到的，而不是通过隐藏的任务特定手工调参获得的。对于 task-adaptive 结果，校准与验证会按任务分别进行；对于 shared-policy 结果，则会先选出一个校准后的共享配置，再将其复用于多个评测任务。

在下文实验中，我们从同一基准集合中抽取一个小规模校准子集以及一个与之不重叠的验证子集，并使用固定随机种子。这一轻量协议已经足以显式暴露不同任务在预算分配偏好上的差异，同时其调参成本相对于完整评测可以忽略不计。

本文主要回答三个问题。第一，差分稀疏在相同平均压缩目标下是否优于统一稀疏预算？第二，完整的联合设计是否优于顺序式稀疏加量化基线？第三，压缩收益能否转化为实际的端到端解码加速？为回答这些问题，我们比较 Dense、MUSTAFAR 风格统一稀疏与 JSQKV 的差分稀疏策略，并在完整框架上将 JSQKV 与顺序式 MUSTAFAR+KIVI 基线（记为 **M+K**）比较。压缩配置包括 K/V 均为 50/50 与 70/70 两组设置，并分别搭配 4-bit 与 2-bit 量化。效率测试在 Meta-Llama-3-8B 上进行，输入长度 4096、输出长度 256、batch size 为 1 到 8。

### 3.2 精度结果

#### 差分稀疏单独评估

我们首先在不使用量化和 Hadamard 变换的前提下评估稀疏策略本身，目的是验证：在相同平均稀疏预算下，基于注意力的预算分配是否优于统一稀疏率。表 1 给出了代表性 LongBench 子集上的结果。结果表明，差分稀疏在两个预算下都优于 MUSTAFAR，并且在 70% 稀疏时优势更明显。这说明 JSQKV 的核心直觉是合理的：压缩预算应优先分配给更可能在未来解码中持续有用的词元。

**表 1：Meta-Llama-3-8B 上，差分稀疏与统一稀疏的比较**

| Budget | Method | NarrativeQA | Qasper | GovReport | TREC | LCC | Avg. |
|---|---|---:|---:|---:|---:|---:|---:|
| 50% | Dense | 23.45 | 42.18 | 29.91 | 74.00 | 57.12 | 45.33 |
| 50% | MUSTAFAR | 23.28 | 41.21 | 28.36 | 73.02 | 56.28 | 44.43 |
| 50% | Differential sparsity | **23.36** | **41.56** | **28.79** | **73.40** | **56.72** | **44.77** |
| 70% | Dense | 23.45 | 42.18 | 29.91 | 74.00 | 57.12 | 45.33 |
| 70% | MUSTAFAR | 22.97 | 40.89 | 24.38 | 70.86 | 54.21 | 42.66 |
| 70% | Differential sparsity | **23.18** | **41.02** | **25.10** | **71.58** | **55.02** | **43.21** |

#### 联合稀疏-量化结果

表 2 给出了 Meta-Llama-3-8B 上的详细 LongBench 结果。结果表明，在四组压缩设置下，JSQKV 均优于顺序式 M+K 基线。在压缩较轻时，两者差距相对较小；而当压缩预算更紧时，JSQKV 的优势更明显，尤其体现在 GovReport 和 LCC 上。这一趋势与 JSQKV 的设计动机一致：压缩比越高，稀疏策略、量化方式和运行时执行的协同设计就越重要。

**表 2：Meta-Llama-3-8B 上的详细 LongBench 结果**

| Setting | NarrativeQA | HotpotQA | GovReport | TREC | PCount | LCC | Avg. |
|---|---:|---:|---:|---:|---:|---:|---:|
| Dense | 23.45 | 46.06 | 29.91 | 74.00 | 4.50 | 57.12 | 39.17 |
| M+K, 50/50 + 4-bit | 23.18 | 45.68 | 28.94 | 73.41 | 4.28 | 56.21 | 38.62 |
| JSQKV, 50/50 + 4-bit | **23.28** | **45.81** | **29.08** | **73.55** | **4.36** | **56.35** | **38.74** |
| M+K, 70/70 + 4-bit | 23.02 | 45.31 | 24.86 | 71.08 | 4.01 | 54.52 | 37.13 |
| JSQKV, 70/70 + 4-bit | **23.14** | **45.45** | **25.07** | **71.24** | **4.12** | **54.71** | **37.29** |
| M+K, 50/50 + 2-bit | 22.54 | 45.14 | 28.61 | 73.32 | 4.18 | 48.74 | 37.09 |
| JSQKV, 50/50 + 2-bit | **22.83** | **45.33** | **29.05** | **73.58** | **4.29** | **49.10** | **37.36** |
| M+K, 70/70 + 2-bit | 22.48 | 44.72 | 23.84 | 70.65 | 3.96 | 45.70 | 35.23 |
| JSQKV, 70/70 + 2-bit | **22.71** | **44.88** | **24.18** | **71.02** | **4.10** | **46.24** | **35.52** |

为了验证这种趋势并非某一模型特有，表 3 汇总了四个附加模型上的 LongBench 平均分。结果表明，在 Llama-2、Mistral 和 Qwen 系列上，JSQKV 相对 M+K 的优势都能稳定保持。这说明收益来自联合设计本身，而不是某一模型结构的偶然性。

**表 3：跨模型 LongBench 平均分**

| Model | Dense | M+K 50/50 4-bit | JSQKV 50/50 4-bit | M+K 70/70 4-bit | JSQKV 70/70 4-bit | M+K 50/50 2-bit | JSQKV 50/50 2-bit | M+K 70/70 2-bit | JSQKV 70/70 2-bit |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Llama2-7B | 27.49 | 27.02 | **27.19** | 26.18 | **26.37** | 25.98 | **26.29** | 24.73 | **25.08** |
| Llama2-13B | 27.66 | 27.20 | **27.40** | 26.42 | **26.71** | 26.09 | **26.40** | 24.98 | **25.31** |
| Mistral-7B | 35.70 | 35.08 | **35.31** | 34.02 | **34.37** | 33.56 | **33.94** | 32.18 | **32.61** |
| Qwen2.5-7B | 36.88 | 36.24 | **36.49** | 35.11 | **35.52** | 34.86 | **35.28** | 33.31 | **33.77** |

### 3.3 效率结果

#### 端到端吞吐

表 4 和图 4 给出了 Meta-Llama-3-8B 上的端到端解码吞吐。在 batch size 为 1 时，Dense 仍然最优，因为此时压缩开销尚未被摊薄。从 batch size 为 2 开始，量化路径开始具备竞争力并经常更优。对于 70/70 + 2-bit 配置而言，其最强区间出现在中小 batch 范围：在 batch size 为 6 时，JSQKV 达到 80.38 tokens/s，而 Dense 和 sparse-only 分别为 56.81 和 78.01 tokens/s。当 batch size 继续增大时，工作负载变得更偏计算密集，KV 带宽下降带来的优势会有所收敛。总体上，吞吐结果表明，一旦工作负载足够受带宽限制，联合压缩就可以转化为实际的解码加速。

**表 4：Meta-Llama-3-8B 端到端吞吐（tokens/s）**

| Batch Size | Dense | Sparse50 | Sparse70 | Sparse50+2bit | Sparse70+2bit |
|---|---:|---:|---:|---:|---:|
| 1 | 28.55 | 17.16 | 16.84 | 23.24 | 23.38 |
| 2 | 39.81 | 31.75 | 30.56 | 42.76 | 42.37 |
| 3 | 49.15 | 45.15 | 46.18 | 55.45 | 57.81 |
| 4 | 55.83 | 53.80 | 57.63 | 64.70 | 66.42 |
| 5 | 56.67 | 66.93 | 65.96 | 72.02 | 74.22 |
| 6 | 56.81 | 77.17 | 78.01 | 76.80 | 80.38 |
| 7 | 59.69 | 86.52 | 83.53 | 80.47 | 84.06 |
| 8 | 62.80 | 94.51 | 96.43 | 86.98 | 87.12 |

图注对应：

- **图 4.** Meta-Llama-3-8B 上的代表性端到端吞吐结果。在中小 batch 场景下，70/70 + 2-bit 的压缩路径能够优于 Dense 解码。

#### 压缩开销

压缩是否有效，前提是其自身开销必须可控。表 5 给出了代表性的预填充阶段压缩统计。结果表明，相比 sparse-only 基线，2-bit sparse-quant 路径能够达到更高的压缩率，同时写回开销也更低，因此压缩时间仍具竞争力。例如，在 batch size 为 8 时，Sparse70 将 KV Cache 压缩到 1964.29MB，而 Sparse70+2bit 则进一步压缩到 1176.25MB，并把压缩时间从 5411.29ms 降低到 4293.18ms。这说明，JSQKV 的效率收益不仅来自解码阶段更低的带宽需求，也来自更紧凑的压缩流水线。

**表 5：Meta-Llama-3-8B 上的代表性压缩开销**

| Batch Size | Method | KV Cache Size (MB) | Compression Ratio | Compression Time (ms) |
|---|---|---:|---:|---:|
| 1 | Sparse50 | 2720.16 | 1.51× | 740.80 |
| 1 | Sparse70 | 1964.29 | 2.09× | 716.30 |
| 1 | Sparse50+2bit | 1261.16 | 3.25× | 545.18 |
| 1 | Sparse70+2bit | 1176.25 | 3.48× | 552.86 |
| 8 | Sparse50 | 2720.16 | 1.51× | 5688.23 |
| 8 | Sparse70 | 1964.29 | 2.09× | 5411.29 |
| 8 | Sparse50+2bit | 1261.16 | 3.25× | 4317.88 |
| 8 | Sparse70+2bit | 1176.25 | 3.48× | 4293.18 |

### 3.4 组件消融

为了分析各组件的重要性，表 6 给出了在最激进配置（70/70 稀疏 + 2-bit 量化）下的消融结果。我们将完整方法与三个退化版本比较：用统一稀疏预算替代差分稀疏、移除 Hadamard 变换、以及将 per-token-tile 量化替换为更粗粒度方案。结果表明，这三种变化都会带来性能下降。其中，移除 Hadamard 变换带来的退化最大，说明低比特 key 量化对异常值高度敏感；而完整方法与统一稀疏版本之间的差距也说明，即使在量化条件下，更合理的预算分配依然重要。

**表 6：Meta-Llama-3-8B 上的组件消融（70/70 稀疏 + 2-bit）**

| Method | NarrativeQA | HotpotQA | GovReport | LCC | Avg. |
|---|---:|---:|---:|---:|---:|
| Full JSQKV | **22.71** | **44.88** | **24.18** | **46.24** | **34.50** |
| w/o Differential Sparsity | 22.48 | 44.72 | 23.84 | 45.70 | 34.19 |
| w/o Hadamard | 22.21 | 44.16 | 22.97 | 44.61 | 33.49 |
| w/o Per-Token-Tile | 22.33 | 44.29 | 23.18 | 44.98 | 33.70 |

综合来看，这些结果支持了 JSQKV 的核心观点：高压缩比 KV Cache 压缩要想真正有效，必须把词元选择、数值表示和解码执行视为一个统一系统来设计。

## Related Work

现有面向长上下文解码的 KV Cache 优化工作主要沿两条路径展开。第一条路径通过词元丢弃或稀疏化来减少需要保留的历史信息。StreamingLLM、H\$_2\$O、SnapKV 和 PyramidKV 主要在词元层面选择或保留更重要的历史项，而 ThinK、KVPruner 和 MUSTAFAR 则进一步探索通道级、结构化以及非结构化稀疏。这类工作说明，不同历史词元的重要性并不相同，基于重要性的缓存裁剪能够有效缓解显存容量和带宽压力。

第二条路径通过低比特量化来压缩保留下来的 KV 状态。KVQuant、QuaRot 和 KIVI 分别从超低比特表示、分布重整以及 Key/Value 异构量化等角度降低 KV Cache 的数值表示成本。这类方法能够直接降低缓存占用和访存带宽，但主要关注“如何量化每个缓存元素”，而较少直接处理“哪些历史信息应该保留”这一结构性问题。

总体来看，现有工作已经证明，结构维度压缩和数值维度压缩都有效，但二者大多仍被分开研究。在实际系统中，稀疏化一旦改变了保留词元及访问模式，量化粒度、压缩格式以及解码算子的设计也必须随之调整。因此，联合压缩并不是简单的顺序叠加。JSQKV 正是针对这一缺口提出，通过差分稀疏、Per-Token-Tile 量化以及 sparse-quant 数据格式与算子的协同设计，在高压缩比下获得更稳定的精度--效率权衡。

## 4 讨论与未来工作

尽管 JSQKV 在长上下文场景下取得了较好的精度–效率权衡，但仍有三方面问题有待进一步研究。首先，JSQKV 的有效性受益于显式的词元重要性评估，而非采用统一的稀疏预算。然而，当前的重要性建模仍主要停留在词元级别，对不同层、不同注意力头以及 Key/Value 两类缓存对象之间更细粒度的敏感性仍缺乏刻画。同时，量化部分尚未与更细粒度的敏感度分析和预算分配进行协同设计。未来可进一步探索面向层、头以及 Key/Value 的细粒度重要性建模，以及与之相匹配的联合量化配置策略。

其次，稀疏–量化联合路径并不会在所有服务条件下都保持优势。在我们的实验中，最明显的吞吐收益主要出现在中小 batch 场景，此时系统瓶颈仍主要来自 KV Cache 的访存开销；而随着 batch size 增大，压缩带来的收益会逐渐被反量化、元数据处理以及更高的计算压力所抵消。因此，如何设计更高效的稀疏–量化联合算子，并进一步降低相关执行开销，仍然是该方向中的关键问题。

最后，当前研究的部署范围仍然相对有限。尽管实验已经覆盖多个模型家族和代表性的长上下文设置，但尚未系统考察更长上下文、更复杂交互负载以及更广泛硬件平台下的表现。未来有必要进一步评估，在极长上下文、异构加速器以及更接近生产环境的场景中，这些设计原则是否仍然成立。

## 5 结论

JSQKV 通过差分稀疏、稳定低比特量化以及 bitmap-based sparse-quant 解码路径来降低 KV Cache 开销。实验结果表明，在相同压缩预算下，JSQKV 持续优于顺序式稀疏加量化基线，并进一步说明压缩收益能够转化为实际解码吞吐提升。总体而言，这些结果支持这样一个观点：KV Cache 压缩应被视为系统与算法的协同设计问题，而不是若干独立后处理步骤的简单组合。
