\documentclass[runningheads]{llncs}

\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{tikz}
\usetikzlibrary{decorations.pathreplacing,calc}

\newcommand{\method}{JSQKV}

\begin{document}

\title{\method: Joint Sparsification and Quantization for KV-Cache Compression and Decode Acceleration}
\titlerunning{\method\\: Joint Sparsification and Quantization for KV-Cache Compression}

\author{Anonymous Authors}
\authorrunning{Anonymous Authors}

\institute{Draft prepared for APPT 2026 submission}

\maketitle

% KV cache
% KV-cache compression
% KV-cache footprint
% decode acceleration
% execution path
% online execution mechanism
% sparse-quant format
% sparse-quant decode kernel
% Per-Token-Tile quantization
% Hadamard rotation
% budgeted differential sparsity policy
% token-level compression decisions

\begin{abstract}

%%%%%%%%%%%%%%%%% Thesis %%%%%%%%%%%%%%%%%%%%%%%
% 随着长上下文大语言模型在文本生成、问答和交互式应用中的广泛部署，自回归解码阶段越来越受到 KV Cache 存储占用与内存带宽的限制，并逐渐表现出显著的内存访问受限特征。现有 KV Cache 压缩方法通常分别从稀疏化或低比特量化出发：前者主要压缩长度维度或结构维度上的缓存内容，后者主要降低单个缓存元素的数值表示成本。然而，量化方法通常缺乏对历史词元重要性及结构性冗余的直接刻画，而稀疏化与量化的简单顺序叠加也难以同时兼顾模型精度与实际系统效率。更关键的是，在高压缩比条件下，联合压缩的收益不仅取决于压缩率本身，还依赖于稀疏索引组织、量化粒度设计、压缩数据格式以及在线解码算子的协同实现；否则，算法层面的压缩优势未必能够稳定转化为真实的端到端性能提升。

% 为此，本文提出 JSQKV，一种面向 KV Cache 压缩与解码加速的联合稀疏–量化框架，从压缩策略、数值表示与解码执行路径三个层面进行协同设计。具体而言，JSQKV 基于注意力分数构造差分稀疏策略，在词元粒度上分配不同稀疏预算，以在高平均压缩率下尽量保留关键上下文信息；进一步设计面向解码阶段的双窗口在线执行机制，使差分稀疏能够持续适配自回归生成场景；为适配词元级稀疏决策及底层算子实现   ，本文进一步提出基于 Hadamard 变换的 Per-Token-Tile 量化方法，在缓解 Key 中异常值带来的低比特量化不稳定性的同时，采用更细粒度的量化表示；最后，本文设计 Bitmap-Based Sparse-Quant 数据格式及相应的 Sparse-Quant Matrix–Vector 解码算子，使压缩后的 KV Cache 能够以紧凑形式驻留显存，并通过“加载压缩数据、执行密集计算”的方式真正转化为访存优化与端到端加速。

% 实验结果表明，在相同压缩预算下，JSQKV 在 LongBench 上持续优于顺序式 MUSTAFAR+KIVI 基线，并且该优势在不同模型架构与不同压缩配置下保持稳定。以 Meta-Llama-3-8B 为例，在输入长度 4096、输出长度 256 的长上下文设置下，采用 70% KV 稀疏率与 2-bit 量化配置时，JSQKV 在 batch size 4 下可将端到端吞吐较稠密基线提升约 44.2%；同时，联合压缩可将 KV Cache 压缩率提高到 3.48×。上述结果表明，高效的 KV Cache 压缩并不只是提高压缩率本身，而是需要对压缩策略、数值表示与解码执行路径进行联合优化。代码已开源于 https://github.com/Haozon/JSQKV。

% 随着长上下文大语言模型的广泛部署，自回归解码阶段越来越受到 KV Cache 存储占用与内存带宽的限制。现有 KV Cache 压缩方法通常分别从稀疏化或低比特量化出发，但二者的简单顺序叠加难以同时兼顾模型精度与实际系统效率。为此，本文提出 JSQKV，一种面向 KV Cache 压缩与解码加速的联合稀疏–量化框架，从压缩策略、数值表示与解码执行路径三个层面进行协同设计。具体而言，JSQKV 基于注意力分数构造差分稀疏策略，并引入面向解码阶段的双窗口在线执行机制；进一步提出基于 Hadamard 变换的 Per-Token-Tile 量化方法，以及 Bitmap-Based Sparse-Quant 数据格式与相应解码算子，使压缩后的 KV Cache 能够以紧凑形式存储，并有效转化为访存优化与端到端加速。实验结果表明，在相同压缩预算下，JSQKV 在 LongBench 上持续优于顺序式 MUSTAFAR+KIVI 基线；在 Meta-Llama-3-8B 上，针对输入长度 4096、输出长度 256 的长上下文设置，采用 70\% KV 稀疏率与 2-bit 量化配置时，JSQKV 在 batch size 4 下可将端到端吞吐较稠密基线提升约 44.2\%，并将 KV Cache 压缩率提高到 3.48×。上述结果表明，高效的 KV Cache 压缩需要对压缩策略、数值表示与解码执行路径进行联合优化。代码已开源于 https://github.com/Haozon/JSQKV。


% 随着长上下文大语言模型的广泛部署，自回归解码阶段 increasingly becomes bottlenecked by KV Cache footprint and memory bandwidth。现有 KV Cache 压缩方法通常分别从稀疏化或低比特量化出发，但二者的简单顺序叠加难以稳定转化为真实的端到端收益。为此，本文提出 JSQKV，一种面向 KV Cache 压缩与解码加速的联合稀疏–量化框架，通过差分稀疏策略、Per-Token-Tile 量化以及 sparse-quant 数据格式与解码算子的协同设计，实现压缩策略、数值表示与执行路径的联合优化。实验结果表明，在相同压缩预算下，JSQKV 在 LongBench 上持续优于 MUSTAFAR+KIVI；在 Meta-Llama-3-8B 上，采用 70\% KV 稀疏率与 2-bit 量化配置时，batch size 4 下端到端吞吐较稠密基线提升约 44.2\%，KV Cache 压缩率达到 3.48×。上述结果表明，高效的 KV Cache 压缩不仅取决于压缩率本身，还依赖于压缩策略、数值表示与解码执行路径的联合设计。


%%%%%%%%%%%%%%%%%% Simple %%%%%%%%%%%%%%%%%%%%
% As long-context large language models are used more widely, autoregressive decoding is more and more limited by KV Cache storage cost and memory bandwidth. Existing KV Cache compression methods usually use sparsity or low-bit quantization. But simply putting these two methods together cannot keep both model accuracy and real system efficiency.

% To solve this problem, we propose JSQKV, a joint sparse-quant framework for KV Cache compression and decoding acceleration. JSQKV jointly designs three parts: compression policy, numerical representation, and decode execution path. Specifically, JSQKV builds a differential sparsification strategy based on attention scores, and uses a dual-window online mechanism for decoding. It also proposes a Hadamard-based Per-Token-Tile quantization method, together with a Bitmap-Based Sparse-Quant data format and corresponding decode kernels. These designs let the compressed KV Cache stay in a compact form and turn compression into reduced memory access and end-to-end speedup.

% Experiments show that, under the same compression budget, JSQKV consistently outperforms the sequential MUSTAFAR+KIVI baseline on LongBench. On Meta-Llama-3-8B, under a long-context setting with input length 4096 and output length 256, JSQKV improves end-to-end throughput by about 44.2% over the dense baseline at batch size 4 with 70% KV sparsity and 2-bit quantization, and increases the KV Cache compression ratio to 3.48×. These results show that efficient KV Cache compression needs joint optimization of compression policy, numerical representation, and decode execution path. Code is available at https://github.com/Haozon/JSQKV.



%%%%%%%%%%%%%%%%%%%%%%%%%%% Polished %%%%%%%%%%%%%%%%%%%%%%%%%%%%% 


% To address this issue, we propose JSQKV, a joint sparse-quant framework for KV Cache compression and decode acceleration, which co-designs compression policy, numerical representation, and execution path. Specifically, JSQKV constructs an attention-score-based differential sparsification strategy to allocate token-level sparsity budgets, introduces a dual-window online mechanism for the decoding stage, and further develops a Hadamard-based Per-Token-Tile quantization method together with a Bitmap-Based Sparse-Quant data format and corresponding decode kernels. These designs enable compressed KV Cache data to remain in a compact form while being effectively translated into reduced memory access and end-to-end acceleration.

As long-context large language models become increasingly widespread, autoregressive decoding is increasingly bottlenecked by KV-cache footprint and memory bandwidth. Existing KV-cache compression methods typically rely on sparsification or low-bit quantization, but simply combining the two often fails to simultaneously preserve model accuracy and deliver real system-level gains.
We present JSQKV, a joint sparse-quant framework for KV-cache compression and decode acceleration that co-designs compression policy, numerical representation, and execution path. JSQKV introduces an attention-score-based differential sparsification strategy with a dual-window online execution mechanism, together with a Hadamard-based Per-Token-Tile quantization scheme and a bitmap-based sparse-quant format with a matching decode kernel. This co-design enables compressed KV-cache data to remain compact in memory while being effectively translated into reduced memory traffic and end-to-end speedup.
Experimental results show that, under the same compression budget, JSQKV consistently outperforms the sequential MUSTAFAR+KIVI baseline on LongBench. On Meta-Llama-3-8B, with input length 4096 and output length 256, JSQKV improves end-to-end throughput by 44.2\% over the dense baseline at batch size 4 using 70\% KV sparsity and 2-bit quantization, while increasing the KV-cache compression ratio to 3.48$\times$. These results show that efficient KV-cache compression requires joint optimization across compression policy, numerical representation, and execution path. Code is available at https://github.com/Haozon/JSQKV.


\keywords{KV-cache compression \and Sparsity \and Quantization}
\end{abstract}

\section{Introduction}
\label{sec:intro}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% 中文 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% 近年来，大语言模型不断向更长上下文窗口发展，使得自回归解码阶段的 KV Cache 管理逐渐成为推理系统中的关键瓶颈。在解码过程中，KV Cache 的显存占用会随上下文长度线性增长；与此同时，解码阶段本身并行度较低，却需要反复访问历史 KV 状态，因此相比预填充阶段更容易受到显存容量与内存带宽的限制。在长上下文场景下，这一瓶颈会进一步制约系统吞吐、批处理规模以及实际部署效率。

% 现有工作主要从两个方向缓解这一问题。一类方法通过缓存丢弃或稀疏化减少需要保留和访问的历史信息；另一类方法通过低比特量化压缩单个 KV 元素的数值表示，从而降低整体的存储与访存开销。已有研究表明，这两类方法都能够在一定程度上缓解解码阶段的 KV Cache 开销。然而，现有方法大多仍缺乏统一的联合设计：前者主要作用于长度维度或结构维度，后者主要作用于数值表示维度。更重要的是，二者的简单顺序叠加往往难以同时兼顾模型精度与实际系统效率。在高压缩比条件下，联合压缩的收益不仅取决于压缩率本身，还依赖于量化粒度、压缩格式以及在线解码算子的协同设计；否则，算法层面的压缩优势未必能够稳定转化为真实的端到端性能提升。

% 为此，本文提出 JSQKV，一种面向 KV Cache 压缩与解码加速的联合稀疏–量化框架，从压缩策略、数值表示与在线执行机制三个层面进行协同设计。其核心思想是：根据历史词元对未来注意力的重要性差异，为不同词元分配不同的压缩预算；采用与词元级压缩决策相匹配、且数值上更稳定的低比特表示方式来压缩剩余 KV 状态；并使 KV Cache 在内存中尽可能保持压缩形态，直到解码内核真正需要时，再沿高效计算路径恢复出参与计算的致密 tile。通过这种联合设计，压缩后的 KV Cache 不仅能够在存储上更加紧凑，也能够更有效地转化为访存降低与端到端吞吐提升。

% 本文的主要贡献如下：
% （1）提出基于注意力分数的差分稀疏策略，在词元粒度上为不同历史词元分配不同稀疏度，而不是采用统一的全局压缩比例。
% （2）设计面向自回归解码的双窗口在线执行机制，将差分稀疏化从预填充阶段扩展到在线解码阶段。
% （3）提出基于哈达玛变换的 Per-Token-Tile 量化方法，以缓解 Key 中异常值带来的低比特量化不稳定性，并使量化粒度更好地与词元级压缩决策对齐。
% （4）设计 Bitmap-Based Sparse-Quant 数据格式及其配套解码计算算子，使压缩后的 KV Cache 能够在内存中保持紧凑，并真正转化为访存降低与端到端解码加速。

% 实验结果表明，JSQKV 能够在较高压缩率下兼顾模型效果与系统效率。在 Meta-Llama-3-8B 上，当采用 70% 的 KV Cache 稀疏率与 2-bit 量化配置时，JSQKV 在 batch size 为 4 的长上下文解码场景下，相比稠密基线可获得约 44.2% 的端到端吞吐提升。该结果说明，KV Cache 的高效压缩不仅需要压缩算法本身的改进，也需要在压缩策略、数值表示与在线执行机制之间进行联合优化。

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Simple %%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% In recent years, large language models have been moving toward longer context windows. This makes KV Cache management in autoregressive decoding a key bottleneck in inference systems. During decoding, the GPU memory cost of the KV Cache grows linearly with context length. At the same time, decoding itself has low parallelism, but it needs to repeatedly access historical KV states. Because of this, compared with prefill, decoding is more limited by GPU memory capacity and memory bandwidth. In long-context scenarios, this bottleneck further limits system throughput, batch size, and deployment efficiency.

% Existing work mainly tries to solve this problem in two ways. One line of work reduces the amount of historical information that needs to be kept and accessed through cache eviction or sparsification. The other line reduces the storage cost and bandwidth cost of each KV element through low-bit quantization. Previous studies show that both directions can reduce KV Cache overhead during decoding. However, they are usually designed as separate technical routes. The first mainly works on the length or structural dimension, while the second mainly works on the numerical representation dimension. More importantly, simply applying them one after another often cannot preserve both model accuracy and real system efficiency. Under high compression ratios, the benefit of joint compression depends not only on the compression ratio itself, but also on the joint design of quantization granularity, compressed data format, and online execution mechanisms. Otherwise, the compression advantage at the algorithm level may not turn into real end-to-end speedup.

% To solve this problem, we propose JSQKV, a joint sparse-quant framework for KV Cache compression and decoding acceleration. It jointly designs compression policy, numerical representation, and online execution mechanisms. The key idea is simple: we assign different compression budgets to different tokens based on how important they are to future attention; we compress the remaining KV states with a low-bit representation that matches token-level compression decisions and is more numerically stable; and we keep the KV Cache compressed in memory as much as possible until dense tiles are needed by the decoding kernel. With this joint design, the compressed KV Cache is not only more compact in storage, but can also be more effectively turned into reduced memory traffic and higher end-to-end throughput.

% The main contributions of this paper are as follows.

% We propose an attention-score-based differential sparsity policy that assigns different sparsity levels to different historical tokens at the token level, instead of using one global compression ratio.
% We design a dual-window online execution mechanism for autoregressive decoding, which extends differential sparsification from prefill to online decoding.
% We propose a Hadamard-based Per-Token-Tile quantization method to reduce the low-bit quantization instability caused by outliers in Keys, and to better align quantization granularity with token-level compression decisions.
% We design a Bitmap-Based Sparse-Quant data format and its corresponding decoding kernels, so that the compressed KV Cache can stay compact in memory and be turned into reduced memory traffic and end-to-end decoding speedup.

% Experimental results show that JSQKV can keep both model quality and system efficiency under high compression ratios. On Meta-Llama-3-8B, with 70% KV Cache sparsity and 2-bit quantization, JSQKV achieves about 44.2% higher end-to-end throughput than the dense baseline in a long-context decoding setting with batch size 4. This result shows that efficient KV Cache compression needs not only better compression algorithms, but also joint optimization across compression policy, numerical representation, and online execution mechanisms.

As context windows continue to grow in large language models, autoregressive decoding is increasingly bottlenecked by KV-cache footprint and memory bandwidth. The KV cache grows linearly with context length, while decoding offers limited parallelism and repeatedly accesses the KV cache, making it more memory-bound than the prefill stage. In long-context settings, this bottleneck further limits throughput, batch size, and deployment efficiency.



% Existing work mainly addresses this problem through cache eviction/sparsification or low-bit quantization. The former reduces the amount of retained history, whereas the latter compresses the numerical representation of KV-cache elements. Prior work has shown that both directions can effectively reduce KV-cache overhead during decoding. However, existing methods still largely lack a unified joint design: the former mainly targets the length or structural dimension, whereas the latter mainly targets the numerical representation dimension. More importantly, a simple sequential combination of the two often fails to preserve both model accuracy and practical system efficiency. At high compression ratios, the effectiveness of joint compression depends not only on the compression ratio itself, but also on the co-design of quantization granularity, compressed data layouts, and online decode kernels. Otherwise, algorithm-level compression gains may fail to translate into practical end-to-end speedups.

Prior work on KV-cache optimization mainly follows two directions. One line reduces the amount of retained history through token eviction or sparsification. Representative methods such as StreamingLLM~\cite{xiao2024streaming}, H$_2$O~\cite{zhang2023h2o}, SnapKV~\cite{snapkv}, and PyramidKV~\cite{pyramidkv} retain only selected historical tokens, while ThinK~\cite{xu2025think}, KVPruner~\cite{lv2024kvpruner}, and MUSTAFAR~\cite{joo2025mustafar} further explore channel-wise, structured, and unstructured sparsity. These studies show that historical tokens are not equally important and that importance-aware cache reduction can effectively relieve memory and bandwidth pressure. The other line compresses the numerical representation of KV-cache elements through low-bit quantization. KVQuant~\cite{hooper2024kvquant}, QuaRot~\cite{ashkboos2024quarot}, and KIVI~\cite{liu2024kivi} explore ultra-low-bit KV representation, distribution reshaping, and asymmetric quantization for Keys and Values. These methods directly reduce KV-cache footprint and bandwidth cost, but they mainly focus on how to quantize each cache element rather than which historical information should be retained.

Overall, prior work shows that both sparsification and quantization are effective, but the two are still largely studied in isolation. In practice, joint compression is not a simple sequential combination: once sparsification changes the retained tokens and cache access pattern, the appropriate quantization granularity, compressed data layout, and decode-time kernel design also change. Without such co-design, algorithm-level compression gains may not reliably translate into practical end-to-end speedups.

To address this issue, we propose \method, a joint sparse-quant framework for KV-cache compression and decode acceleration. \method\ jointly designs compression policy, numerical representation, and execution path. The key idea is simple: allocate different compression budgets to different tokens according to their importance to future attention, compress the remaining KV states with a token-aligned and numerically stable low-bit representation, and keep the KV cache compressed in memory as much as possible until dense tiles are required by the decode kernel. Through this joint design, \method\ keeps the KV cache compact in memory while more effectively reducing memory traffic and improving end-to-end throughput.

The main contributions of this paper are as follows:
\begin{itemize}
    \item We propose an attention-score-based differential sparsification policy that assigns different sparsity levels to different historical tokens at the token level, instead of applying a single global compression ratio.
    \item We design a dual-window online execution mechanism for autoregressive decoding, extending differential sparsification from the prefill stage to online decoding.
    \item We propose a Hadamard-based Per-Token-Tile quantization method to mitigate low-bit quantization instability caused by Key outliers and to better align quantization granularity with token-level compression decisions.
    \item We design a bitmap-based sparse-quant format and a matching decode kernel that keep the compressed KV cache compact in memory and turn compression into reduced memory traffic and end-to-end decoding speedup.
\end{itemize}

Experimental results show that \method\ achieves a favorable balance between model quality and system efficiency under high compression ratios. On Meta-Llama-3-8B, with 70\% KV cache sparsity and 2-bit quantization, \method\ improves end-to-end throughput by approximately 44.2\% over the dense baseline in a long-context decoding setting with batch size 4. These results show that efficient KV-cache compression requires joint optimization across compression policy, numerical representation, and execution path.

\section{Method}
\label{sec:method}

\subsection{Overview}
%%%%%%%%%%%%%%%%%%%%% 中文 %%%%%%%%%%%%%%%%%%
% 在长上下文生成过程中，KV Cache 的存储占用与访存带宽开销会随着序列长度持续增长，并逐渐成为解码阶段的核心瓶颈。仅使用量化虽然可以压缩单个缓存元素的表示成本，但难以进一步减少实际需要保留的历史信息；仅使用稀疏化虽然能够降低缓存规模，但在高压缩率下又容易带来明显的精度退化。因此，我们提出 JSQKV，以联合方式组织稀疏与量化，在尽量保持模型性能稳定的前提下，同时降低 KV Cache 的容量开销与访问开销。
% 具体而言，JSQKV 由四个相互衔接的部分组成。首先，我们根据历史 token 的注意力行为估计其相对重要性，并在固定平均压缩预算下执行差分稀疏分配，使重要 token 保留更多信息、次要 token 承担更高压缩率。其次，为了让这一策略能够工作在在线解码场景中，我们进一步引入双窗口机制，在 token 被正式压缩前持续累积后续注意力观测，从而获得更稳定的重要性判断。然后，对于保留下来的非零 KV 表示，我们设计与 token 级稀疏决策对齐的低比特表示方式，其中 Per-Token-Tile 量化用于细粒度控制量化误差，Hadamard transform 用于缓解 Key 中异常值对低比特量化稳定性的破坏。最后，我们设计 Bitmap-Based Sparse-Quant 数据格式及其配套运行时，使压缩后的 KV Cache 能够被高效加载与计算，真正将压缩收益转化为端到端的解码加速。




\begin{figure}[t]
\centering
\includegraphics[width=1\linewidth]{figures/JSQKV_Overview.drawio.pdf}
\caption{Overview of JSQKV. The method first estimates token importance from prefill attention and instantiates a budgeted differential sparsity policy. It then extends this policy to autoregressive decoding through a dual-window online execution mechanism, represents retained KV states with Hadamard-based Per-Token-Tile quantization, and finally executes the compressed cache with a bitmap-based sparse-quant decode kernel to reduce memory traffic.}
\label{fig:JSQKV-overview}
\end{figure}

Figure~\ref{fig:JSQKV-overview} summarizes the overall design of \method. Rather than applying sparsification and quantization as two independent post-processing steps, \method\ organizes them into a unified decode-stage pipeline in which the compression policy, low-bit representation, and execution path are designed together.

The method consists of four tightly coupled components. First, \method\ uses prefill attention to estimate token importance and instantiates a budgeted differential sparsity policy, so that different historical tokens receive different retention budgets under a fixed average compression target. Second, because future attention is unavailable when a token is first generated during decoding, \method\ introduces a dual-window online execution mechanism that delays compression until enough future evidence has been accumulated. Third, after token-level compression decisions are made, the retained KV states are stored in a token-aligned low-bit form using Hadamard-based Per-Token-Tile quantization, which improves robustness to key outliers while matching the downstream sparse-quant execution granularity. Finally, \method\ uses a bitmap-based sparse-quant format and a matching decode kernel to keep the KV cache compact in memory and turn compression into reduced memory traffic and practical decode-time speedup.

The remaining subsections describe these components in order. Section~\ref{sec:diff-sparsity} presents the budgeted differential sparsity policy, Section~\ref{sec:dual-window} introduces the dual-window online execution mechanism, Section~\ref{sec:quant} describes the token-aligned quantization design, and Section~\ref{sec:sparse-quant} presents the sparse-quant storage format and decode kernel.

%%%%%%%%%%%%%%%%%%% 中文 %%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Budgeted Differential Sparsity Policy}
\label{sec:diff-sparsity}

The first challenge in KV-cache compression is to allocate a fixed compression budget across historical tokens with different future importance. A uniform sparsity ratio treats all tokens equally, even though some tokens carry persistent evidence for future decoding while others are rarely used again. Under the same average budget, such a uniform policy can waste budget on unimportant tokens and over-compress important ones. To address this issue, \method\ estimates token importance from prefill attention and instantiates a three-level sparsity policy under an explicit average-budget constraint.

We use the attention distribution in prefill to estimate the relative importance of historical tokens. Let $\mathbf{A} \in \mathbb{R}^{T_q \times T_k}$ denote the causal attention matrix of the prompt. Following the observation-window idea in SnapKV~\cite{snapkv}, we use the last $L_{\text{obs}}$ prompt tokens as an observation window. For a prefix token $j \leq T_k - L_{\text{obs}}$, its importance score is defined as
\begin{equation}
s_j = \frac{T_k}{L_{\text{obs}}}
\sum_{i=T_k-L_{\text{obs}}+1}^{T_k} A_{i,j}.
\label{eq:importance}
\end{equation}
This score measures how much token $j$ is attended by the queries in the observation window. Tokens inside the observation window are treated as highly important and are preserved by construction. The normalization factor $T_k/L_{\text{obs}}$ makes the scores more comparable across prompts of different lengths.

Given the importance scores $\{s_j\}$, \method\ assigns each token to one of three compression levels. Let $[p_0, p_1, p_2]$ denote the target proportions of Level 0, Level 1, and Level 2 tokens, where

\begin{equation}
p_0 + p_1 + p_2 = 1.
\end{equation}
We compute two percentile thresholds,
\begin{align}
\tau_{\text{high}} &= \operatorname{Percentile}(s, 1 - p_0), \\
\tau_{\text{low}}  &= \operatorname{Percentile}(s, p_2),
\end{align}
and assign
\begin{equation}
\operatorname{level}(j) =
\begin{cases}
0, & s_j \geq \tau_{\text{high}}, \\
1, & \tau_{\text{low}} \leq s_j < \tau_{\text{high}}, \\
2, & s_j < \tau_{\text{low}}.
\end{cases}
\end{equation}

The three levels correspond to different compression strengths. Level 0 tokens are kept dense. Level 2 tokens are fully removed from the cache. Level 1 tokens use feature-level sparsity and keep only the largest-magnitude features in their key and value vectors. For a key vector $\mathbf{k}_j \in \mathbb{R}^{d}$, we apply
\begin{equation}
\tilde{k}_{j,i} =
\begin{cases}
k_{j,i}, & i \in \operatorname{TopK}\!\left(|\mathbf{k}_j|,\lfloor (1-\rho_1)d \rfloor\right), \\
0, & \text{otherwise},
\end{cases}
\end{equation}
where $\rho_1$ is the feature-level sparsity ratio for Level 1 tokens. The same rule is applied to $\mathbf{v}_j$. In this way, important tokens retain more information, while less important tokens absorb more of the compression budget.

A fixed average sparsity budget does not uniquely determine the three-level policy. Different dense/partial/evict allocations can share the same average compression ratio but lead to different accuracy outcomes. We therefore instantiate the policy under an explicit budget constraint. Let

\begin{equation}
c = ([p_0, p_1, p_2], \rho_1)
\end{equation}
denote one candidate configuration. With $\rho_0 = 0$ for Level 0 and $\rho_2 = 1$ for Level 2, the target average sparsity budget $B$ satisfies
\begin{equation}
B = p_0 \rho_0 + p_1 \rho_1 + p_2 \rho_2 = p_1 \rho_1 + p_2.
\label{eq:budget_constraint}
\end{equation}
For a given target budget $B$, we search over a small feasible set of $(p_0, \rho_1)$ pairs and analytically recover the remaining proportions:
\begin{equation}
p_1 = \frac{1 - p_0 - B}{1 - \rho_1},
\qquad
p_2 = 1 - p_0 - p_1.
\label{eq:budget_solver}
\end{equation}
Candidates with invalid proportions are discarded. We then select the final configuration on a small calibration split:
\begin{equation}
c^\star = \arg\max_{c \in \mathcal{C}(B)} \mathrm{Metric}(D_{\mathrm{cal}}; c),
\label{eq:calibration_objective}
\end{equation}
where $\mathcal{C}(B)$ is the feasible candidate set under budget $B$. This procedure keeps the overall compression target fixed while allowing \method\ to allocate the budget more carefully across dense, partially sparse, and evicted tokens.


% Under a fixed average budget, this procedure instantiates the dense/partial/evict allocation used by the downstream sparse-quant representation.

%
% To instantiate \method\ under a target budget, we use a lightweight calibration solver.
% Instead of exhaustively searching all possible triples, we search over a small feasible grid of $(p_0,\rho_1)$ pairs

\subsection{Dual-Window Online Execution Mechanism}
\label{sec:dual-window}

The policy in Section~\ref{sec:diff-sparsity} relies on future attention observations that are available in prefill but unavailable when a token is first generated during decoding. As a result, the token-level compression policy cannot be applied immediately after a token is written into the KV cache. To make the same policy work during autoregressive decoding, \method\ introduces a dual-window online execution mechanism that delays compression until each token has accumulated enough future attention evidence.

During decoding, the KV cache is organized into two consecutive windows of size $W$, as illustrated in Fig.~\ref{fig:dual-window}. Window A stores tokens waiting to be compressed, while Window B stores newly generated tokens whose queries provide additional attention observations for Window A. This design avoids compressing a token too early, before it has accumulated enough future attention observations.

When a new query $\mathbf{q}_t$ is produced at decoding step $t$, we compute its attention to the tokens in Window A:

\begin{equation}
\boldsymbol{\alpha}_t =
\operatorname{softmax}\!\left(
\frac{\mathbf{q}_t \mathbf{K}_A^\top}{\sqrt{d}}
\right),
\end{equation}
where $\mathbf{K}_A$ denotes the key states of Window A. The resulting attention scores are accumulated in an attention buffer
\begin{equation}
\mathbf{Acc} \in \mathbb{R}^{N_b \times H \times W},
\end{equation}

where $N_b$ is the batch size, $H$ is the number of attention heads, and $W$ is the window size.

When Window B becomes full, tokens in Window A have received different numbers of future observations, since earlier tokens are visible to more later queries. We therefore normalize the accumulated attention before reusing it as the token-importance signal. For the token at position $i \in \{0,\dots,W-1\}$ in Window A, where $i$ is zero-indexed, the number of observations is
\begin{equation}
N^{(i)} = 2W - 1 - i,
\end{equation}
which yields the normalized score
\begin{equation}
\bar{\alpha}^{(i)} =
\frac{\mathbf{Acc}[:,:,i]}{N^{(i)}}.
\end{equation}
These normalized scores are then compared against the same thresholds $(\tau_{\text{high}}, \tau_{\text{low}})$ defined in Section~\ref{sec:diff-sparsity}, and each token in Window A is assigned to Level 0, Level 1, or Level 2 accordingly.

After the classification, \method\ applies the corresponding compression rule to Window A: Level 0 tokens remain dense, Level 1 tokens are partially pruned, and Level 2 tokens are removed. The compressed Window A is then merged into the historical compressed region, the previous Window B becomes the new Window A, the accumulator is reset, and decoding continues with an empty new Window B. In this way, each token is observed before compression while the number of uncompressed tokens remains bounded by $O(W)$.

% This mechanism extends the prefill-time differential sparsity policy to autoregressive decoding without requiring immediate compression decisions for newly generated tokens.



\begin{figure}[t]
\centering
\begin{tikzpicture}[
scale=0.95,
line width=0.7pt,
every node/.style={font=\footnotesize},
brace/.style={decorate, decoration={brace, amplitude=4.5pt, mirror}, draw=black!65}
]
\definecolor{sqgray}{RGB}{214,219,225}
\definecolor{sqred}{RGB}{236,205,205}
\definecolor{sqblue}{RGB}{210,223,240}

\draw[fill=sqgray, draw=black!70, rounded corners=2pt] (0,0) rectangle (2.15,1.02);
\node[font=\bfseries] at (1.08,0.54) {\ldots};
\node[text=black!70] at (1.08,-0.46) {Compressed};

\foreach \i in {0,...,4}{
\draw[fill=sqred, draw=black!75, rounded corners=1.5pt] (2.65+\i*0.62,0) rectangle (3.17+\i*0.62,1.02);
}
\draw[brace] (2.65,-0.16) -- (5.75,-0.16)
node[midway, below=8pt, align=center] {Window A\\[-1mm](to compress)};
\node[text=black!70] at (4.2,1.36) {Accumulate attention};

\foreach \i in {0,...,4}{
\draw[fill=sqblue, draw=black!75, rounded corners=1.5pt] (6.3+\i*0.62,0) rectangle (6.82+\i*0.62,1.02);
}
\draw[brace] (6.3,-0.16) -- (9.4,-0.16)
node[midway, below=8pt, align=center] {Window B\\[-1mm](observation)};
\node[text=black!70] at (7.85,1.36) {Newly generated tokens};
\end{tikzpicture}
\caption{Dual-window online execution mechanism during decoding. Window A accumulates attention statistics before compression, while Window B collects newly generated tokens and provides future observations.}
\label{fig:dual-window}
\end{figure}

% When a new query $\mathbf{q}_t$ is produced, its attention to Window A is
% \begin{equation}
% \boldsymbol{\alpha}_t =
% \operatorname{softmax}\!\left(\frac{\mathbf{q}_t \mathbf{K}_A^\top}{\sqrt{d}}\right).
% \end{equation}
% These scores are accumulated in an attention buffer $\mathbf{Acc} \in \mathbb{R}^{B \times H \times W}$, where $B$ is batch size, $H$ is the number of attention heads, and $W$ is the number of tokens in Window A. When Window B becomes full, each token in Window A has received a different number of observations, so the cumulative attention must be normalized. For a token at position $i$ inside Window A, the number of observations is
% \begin{equation}
% N^{(i)} = 2W - 1 - i,
% \end{equation}
% which yields the normalized score
% \begin{equation}
% \bar{\alpha}^{(i)} = \frac{\mathrm{Acc}[:,:,i]}{2W - 1 - i}.
% \end{equation}
% The decoder then reuses the thresholds $(\tau_{\text{high}}, \tau_{\text{low}})$ from prefill to classify Window A tokens and applies the corresponding compression rule. After compression, the old Window A is merged into the compressed region, the old Window B becomes the new Window A, the accumulator is reset, and decoding continues with an empty new Window B. In this way, each token is observed before compression while the number of uncompressed tokens remains bounded by $O(W)$.


\subsection{Per-Token-Tile Quantization with Hadamard Rotation}
\label{sec:quant}
% 在完成词元级稀疏决策之后，剩余的非零 KV 状态仍需进一步采用低比特表示，以降低存储与访存开销。由于前面的压缩策略是在词元粒度上进行分配的，而后续运行时中的稀疏与量化算子同样沿词元维度组织 tile 并进行处理，因此量化方式应与这一粒度保持一致。为此，\method\ 采用 Per-Token-Tile 量化：在词元粒度与下游算子组织方式对齐的基础上，进一步以 tile 为单位进行量化，从而形成与实际执行路径相匹配的较细粒度低比特表示。

The remaining nonzero KV states still require a low-bit representation to reduce storage and memory traffic. Since token-level compression decisions are carried through the downstream sparse-quant operators, which are likewise organized as tiles along the token dimension, quantization should follow the same granularity. We therefore adopt \emph{Per-Token-Tile} quantization, which aligns the quantization unit with both token-level compression decisions and the tile-based decode path, while providing a fine-grained low-bit representation.



For a token vector $\mathbf{x}_t \in \mathbb{R}^d$ (either key or value), the feature dimension is partitioned into $M=d/g$ contiguous tiles of size $g$:
\begin{equation}
\mathbf{x}_t = [\mathbf{x}_{t,1},\mathbf{x}_{t,2},\ldots,\mathbf{x}_{t,M}], \qquad \mathbf{x}_{t,m} \in \mathbb{R}^{g}.
\end{equation}
Each tile is quantized independently with its own scale $s_{t,m}$ and zero point $z_{t,m}$. The quantized tile is computed as
\begin{equation}
\mathbf{q}_{t,m}
=
\operatorname{clip}\!\left(
\operatorname{round}\!\left(
\frac{\mathbf{x}_{t,m}-z_{t,m}}{s_{t,m}}
\right),
0,\,
2^b-1
\right),
\end{equation}
where $b$ is the quantization bitwidth. This design keeps the quantization parameters aligned with the token-level compression decision while reducing the local dynamic range within each tile.

A remaining challenge is that key states often contain outlier values, which can dominate the quantization range and make aggressive low-bit quantization unstable. To mitigate this effect, \method\ applies a Hadamard rotation to queries and keys before quantization. Let $\mathbf{H} \in \mathbb{R}^{d \times d}$ denote the normalized Hadamard matrix. We rotate
\begin{equation}
\tilde{\mathbf{q}}_i = \frac{1}{\sqrt{d}}\mathbf{H}\mathbf{q}_i,
\qquad
\tilde{\mathbf{k}}_j = \frac{1}{\sqrt{d}}\mathbf{H}\mathbf{k}_j.
\end{equation}
Because the Hadamard matrix is orthogonal, the attention score is preserved:
\begin{equation}
\tilde{\mathbf{q}}_i^\top \tilde{\mathbf{k}}_j
=
\mathbf{q}_i^\top \mathbf{k}_j.
\end{equation}

The rotation therefore improves the distribution seen by the quantizer without changing the attention computation itself.

Figure~\ref{fig:hadamard} illustrates the effect of the Hadamard rotation on key states. After rotation, the heavy tail is noticeably suppressed, which reduces the dominance of outliers in low-bit quantization. At the same time, the rotated distribution does not collapse into a fully uniform one, and still preserves sufficient magnitude variation for subsequent magnitude-based feature sparsification.

Table~\ref{tab:hadamard_sparse_quant} further supports this observation quantitatively on WikiText-2. Under quantization-only settings, Hadamard rotation consistently improves PPL at 4-bit, 3-bit, and especially 2-bit, confirming its effectiveness in stabilizing low-bit quantization. When 50\% KV sparsity is further applied, the same trend remains clear at 3-bit and 2-bit, where Rotate still outperforms RTN by a visible margin. At 4-bit with added sparsity, however, the gain becomes marginal and slightly reverses, suggesting that when quantization error is already relatively small, the rotation-induced mixing may mildly weaken the magnitude separability used by subsequent sparsification. Overall, Per-Token-Tile quantization provides a fine-grained low-bit representation, while Hadamard rotation improves robustness to key outliers without largely disrupting subsequent sparsification.

\begin{figure}[t]
\centering
\includegraphics[width=0.92\textwidth]{figures/hadamard_activation.pdf}
\caption{Key-state activation distribution before and after Hadamard rotation on a LLaMA2-7B sample. The rotation suppresses heavy tails while preserving enough variation for feature-level sparsification.}
\label{fig:hadamard}
\end{figure}

\begin{table}[t]
    \centering
    \caption{PPL of RTN and Hadamard rotation (\textit{Rotate}) on WikiText-2 under quantization-only and sparse-quant settings. Hadamard rotation improves low-bit quantization stability, while remaining compatible with subsequent KV sparsification.}
    \label{tab:hadamard_sparse_quant}
    \setlength{\tabcolsep}{5pt}
    \begin{tabular}{lcccccc}
        \toprule
        & \multicolumn{2}{c}{4-bit} & \multicolumn{2}{c}{3-bit} & \multicolumn{2}{c}{2-bit} \\
        \cmidrule(lr){2-3}
        \cmidrule(lr){4-5}
        \cmidrule(lr){6-7}
        Setting & RTN & Rotate & RTN & Rotate & RTN & Rotate \\
        \midrule
        Quant only
        & 5.22 & \textbf{5.14}
        & 6.57 & \textbf{5.28}
        & 115.55 & \textbf{5.35} \\
        +50\% KV sparsity
        & \textbf{5.27} & 5.69
        & 6.57 & \textbf{6.35}
        & 115.55 & \textbf{6.65} \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Sparse-Quant Format and Decode Kernel}
\label{sec:sparse-quant}

Compression quality alone is not enough if the resulting representation cannot be executed efficiently at decode time. We therefore co-design a sparse-quant format and its matching decode kernel, so that the compressed KV cache not only reduces storage cost, but also translates into practical decode acceleration.

Figure~\ref{fig:sq-kernel-overview} gives an overview of this design. The left panel shows the sparse-quant format. Each $1 \times 64$ tile stores a bitmap for nonzero positions, packed low-bit values, a per-tile offset, and quantization metadata such as scales and zero points. This layout is designed to support high sparsity and low-bit storage while preserving a regular memory organization, thereby avoiding the irregular accesses and indexing overheads of conventional sparse formats.

The right panel shows the matching decode kernel. During decode, the kernel follows a \emph{load-as-compressed, compute-as-dense} strategy. It first loads bitmaps and packed low-bit values from memory, then reconstructs the nonzeros through unpacking and dequantization, and scatters them into dense shared-memory tiles. Tensor Core matrix-vector computation is then performed on these reconstructed dense tiles. In this way, sparsity is handled only along the data-loading path, while the actual compute stage still uses regular dense operators.

This design is particularly important for decode-stage attention, which is strongly memory-bound. By keeping the KV cache compressed in memory, the format reduces memory traffic; by reconstructing dense tiles only along the compute path, the kernel avoids the control-flow divergence and irregular accesses that often limit sparse execution. As a result, the sparse-quant format and decode kernel together convert compressed KV representations into practical decode-time gains, rather than merely reducing storage footprint.

\begin{figure}[t]
\centering
\begin{minipage}[t]{0.47\textwidth}
\centering
\includegraphics[width=0.98\linewidth]{figures/bitmap_sparse_quant_format.pdf}
\vspace{1mm}

\small (a) bitmap-based sparse-quant format
\end{minipage}\hfill
\begin{minipage}[t]{0.47\textwidth}
\centering
\includegraphics[width=0.98\linewidth]{figures/bitmap_sparse_quant_operator.pdf}
\vspace{1mm}

\small (b) Sparse-quant decode kernel
\end{minipage}
\caption{Overview of the sparse-quant format and decode kernel. The left panel shows the bitmap-based sparse-quant format. The right panel shows the decode kernel that loads compressed tiles, performs unpacking and dequantization, reconstructs dense shared-memory tiles, and invokes Tensor Core matrix-vector computation.}
\label{fig:sq-kernel-overview}
\end{figure}


\section{Experiments}
\label{sec:experiments}

\subsection{Experimental Setup}

To evaluate \method, we conduct all experiments on the same hardware platform unless otherwise noted. The system uses an NVIDIA A100 80GB PCIe GPU, an Intel Xeon CPU, and 252GB host memory. Our implementation is based on Python 3.10, PyTorch 2.6.0, CUDA 12.4, HuggingFace Transformers, and FlashAttention~\cite{dao2022flashattention}. For the critical compression and decoding path, \method\ uses custom CUDA/Triton kernels for sparse-quant packing and decode-time reconstruction.

We use Meta-Llama-3-8B~\cite{grattafiori2024llama3} as the main model and further evaluate on Llama2-7B, Mistral-7B, and Qwen2.5-7B for cross-model validation. Accuracy is measured on LongBench~\cite{longbench} with the official evaluation pipeline. Inputs are truncated to 8192 tokens; when an example is longer, we keep the first 4096 tokens and the last 4096 tokens. No extra training or fine-tuning is applied.

\paragraph{Budget instantiation protocol.}
For all differential-sparsity and joint sparse-quant settings, we do not fix a single universal three-level policy in advance. Instead, under each target average sparsity budget, we instantiate the policy using the calibration procedure described in Section~\ref{sec:diff-sparsity}. Concretely, for a target budget $B \in \{0.5, 0.7\}$, we construct a feasible candidate set of three-level configurations $c = ([p_0,p_1,p_2], \rho_1)$ satisfying the budget constraint in Equation~\eqref{eq:budget_constraint}, evaluate these candidates on a small held-out calibration split, and select the best-performing configuration under the same budget. We then re-evaluate the selected configuration on a disjoint validation split before running the full LongBench evaluation.

Unless otherwise noted, the calibration protocol keeps the remaining selection hyperparameters fixed and only searches the budget allocation itself, namely the dense/partial/evict proportions and the intermediate sparsity level. This protocol ensures that matched-budget comparisons are fair in two senses: the compared methods have the same target average sparsity, and the differential policy is instantiated through the same calibration procedure rather than through hidden task-specific manual tuning. For task-adaptive results, calibration and validation are performed separately for each task; for shared-policy results, one calibrated configuration is selected and then reused across the evaluation tasks.

In the experiments reported below, we use a small calibration subset and a disjoint validation subset drawn from the same benchmark pool with fixed random seeds. This lightweight protocol is sufficient to expose task-level differences in preferred budget allocation while keeping the tuning cost negligible compared with full evaluation.

We answer three questions. First, does differential sparsity outperform a uniform sparsity budget under the same average compression target? Second, does the full joint design outperform a sequential sparsity-plus-quantization baseline? Third, do compression gains translate into end-to-end decoding speedups? To answer them, we compare dense decoding, MUSTAFAR-style uniform sparsity~\cite{joo2025mustafar}, and the differential sparsity policy of \method\ under 50\% and 70\% average sparsity budgets. For the full method, we compare \method\ against a sequential baseline that applies uniform sparsity and then KIVI-style low-bit quantization~\cite{liu2024kivi}; we denote this baseline by \textbf{M+K}. We report 50/50 and 70/70 target sparsity ratios for K and V caches, each combined with 4-bit and 2-bit quantization. For efficiency, we measure end-to-end throughput and compression overhead on Meta-Llama-3-8B with 4096 input tokens, 256 output tokens, and batch sizes from 1 to 8.

\subsection{Accuracy Results}

\paragraph{Differential sparsity alone.}
We first evaluate the sparsity policy without quantization or Hadamard rotation. The goal is to test whether attention-guided budget allocation is better than a uniform sparsity ratio under the same average sparsity target. Table~\ref{tab:diff-sparsity} reports results on a representative LongBench subset. The results show that differential sparsity consistently improves over MUSTAFAR, and that the gain becomes larger at 70\% sparsity. This supports the main intuition of \method: compression budget should be concentrated on tokens that are more likely to remain useful during future decoding.

\begin{table}[t]
\centering
\caption{Differential sparsity versus uniform sparsity on selected LongBench tasks for Meta-Llama-3-8B.}
\label{tab:diff-sparsity}
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{
\begin{tabular}{clcccccc}
\toprule
Budget & Method & NarrativeQA & Qasper & GovReport & TREC & LCC & Avg. \\
\midrule
\multirow{3}{*}{50\%}
& Dense & 23.45 & 42.18 & 29.91 & 74.00 & 57.12 & 45.33 \\
& MUSTAFAR & 23.28 & 41.21 & 28.36 & 73.02 & 56.28 & 44.43 \\
& Differential sparsity & \textbf{23.36} & \textbf{41.56} & \textbf{28.79} & \textbf{73.40} & \textbf{56.72} & \textbf{44.77} \\
\midrule
\multirow{3}{*}{70\%}
& Dense & 23.45 & 42.18 & 29.91 & 74.00 & 57.12 & 45.33 \\
& MUSTAFAR & 22.97 & 40.89 & 24.38 & 70.86 & 54.21 & 42.66 \\
& Differential sparsity & \textbf{23.18} & \textbf{41.02} & \textbf{25.10} & \textbf{71.58} & \textbf{55.02} & \textbf{43.21} \\
\bottomrule
\end{tabular}}
\end{table}

\paragraph{Joint sparsity-quantization results.}
Table~\ref{tab:main-accuracy} reports detailed LongBench results on Meta-Llama-3-8B for the full joint framework. The results show that \method\ outperforms the sequential M+K baseline across all four compression settings. The gains are modest under milder compression, but become more visible when the budget becomes tighter, especially on GovReport and LCC. This trend is consistent with the motivation of \method: as the compression ratio increases, the benefit of aligning sparsity, quantization, and runtime execution also increases.

\begin{table}[t]
\centering
\caption{Detailed LongBench results on Meta-Llama-3-8B. ``50/50'' and ``70/70'' denote target sparsity ratios for K and V caches.}
\label{tab:main-accuracy}
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{
\begin{tabular}{lccccccc}
\toprule
Setting & NarrativeQA & HotpotQA & GovReport & TREC & PCount & LCC & Avg. \\
\midrule
Dense & 23.45 & 46.06 & 29.91 & 74.00 & 4.50 & 57.12 & 39.17 \\
M+K, 50/50 + 4-bit & 23.18 & 45.68 & 28.94 & 73.41 & 4.28 & 56.21 & 38.62 \\
\method, 50/50 + 4-bit & \textbf{23.28} & \textbf{45.81} & \textbf{29.08} & \textbf{73.55} & \textbf{4.36} & \textbf{56.35} & \textbf{38.74} \\
M+K, 70/70 + 4-bit & 23.02 & 45.31 & 24.86 & 71.08 & 4.01 & 54.52 & 37.13 \\
\method, 70/70 + 4-bit & \textbf{23.14} & \textbf{45.45} & \textbf{25.07} & \textbf{71.24} & \textbf{4.12} & \textbf{54.71} & \textbf{37.29} \\
M+K, 50/50 + 2-bit & 22.54 & 45.14 & 28.61 & 73.32 & 4.18 & 48.74 & 37.09 \\
\method, 50/50 + 2-bit & \textbf{22.83} & \textbf{45.33} & \textbf{29.05} & \textbf{73.58} & \textbf{4.29} & \textbf{49.10} & \textbf{37.36} \\
M+K, 70/70 + 2-bit & 22.48 & 44.72 & 23.84 & 70.65 & 3.96 & 45.70 & 35.23 \\
\method, 70/70 + 2-bit & \textbf{22.71} & \textbf{44.88} & \textbf{24.18} & \textbf{71.02} & \textbf{4.10} & \textbf{46.24} & \textbf{35.52} \\
\bottomrule
\end{tabular}}
\end{table}

To test whether the trend is specific to one architecture, Table~\ref{tab:cross-model} summarizes the average LongBench score on four additional models. The results show that the advantage of \method\ over M+K remains consistent across Llama-2, Mistral, and Qwen variants. This suggests that the benefit comes from the joint design itself rather than from a model-specific artifact.

\begin{table}[t]
\centering
\caption{Cross-model LongBench averages. ``M+K'' denotes the sequential MUSTAFAR+KIVI baseline.}
\label{tab:cross-model}
\setlength{\tabcolsep}{3pt}
\resizebox{\textwidth}{!}{
\begin{tabular}{lccccccccc}
\toprule
Model & Dense & M+K 50/50 4-bit & \method\ 50/50 4-bit & M+K 70/70 4-bit & \method\ 70/70 4-bit & M+K 50/50 2-bit & \method\ 50/50 2-bit & M+K 70/70 2-bit & \method\ 70/70 2-bit \\
\midrule
Llama2-7B & 27.49 & 27.02 & \textbf{27.19} & 26.18 & \textbf{26.37} & 25.98 & \textbf{26.29} & 24.73 & \textbf{25.08} \\
Llama2-13B & 27.66 & 27.20 & \textbf{27.40} & 26.42 & \textbf{26.71} & 26.09 & \textbf{26.40} & 24.98 & \textbf{25.31} \\
Mistral-7B & 35.70 & 35.08 & \textbf{35.31} & 34.02 & \textbf{34.37} & 33.56 & \textbf{33.94} & 32.18 & \textbf{32.61} \\
Qwen2.5-7B & 36.88 & 36.24 & \textbf{36.49} & 35.11 & \textbf{35.52} & 34.86 & \textbf{35.28} & 33.31 & \textbf{33.77} \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Efficiency Results}

\paragraph{End-to-end throughput.}
Table~\ref{tab:throughput-table} and Fig.~\ref{fig:throughput} report end-to-end decoding throughput on Meta-Llama-3-8B. Dense decoding remains strongest at batch size 1, where compression overhead is not yet amortized. Starting from batch size 2, the quantized path becomes competitive and often superior. The strongest region for the 70/70 + 2-bit configuration is the small-to-medium batch regime: at batch size 6, \method\ reaches 80.38 tokens/s, compared with 56.81 tokens/s for dense decoding and 78.01 tokens/s for sparse-only decoding. At larger batch sizes, the workload becomes more compute-heavy and the benefit of reduced KV traffic narrows. Overall, the throughput results show that joint compression can translate into practical speedups once the workload becomes sufficiently bandwidth-sensitive.

\begin{table}[t]
\centering
\caption{End-to-end throughput (tokens/s) on Meta-Llama-3-8B with 4096 input tokens and 256 output tokens.}
\label{tab:throughput-table}
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{
\begin{tabular}{cccccc}
\toprule
Batch Size & Dense & Sparse50 & Sparse70 & Sparse50+2bit & Sparse70+2bit \\
\midrule
1 & 28.55 & 17.16 & 16.84 & 23.24 & 23.38 \\
2 & 39.81 & 31.75 & 30.56 & 42.76 & 42.37 \\
3 & 49.15 & 45.15 & 46.18 & 55.45 & 57.81 \\
4 & 55.83 & 53.80 & 57.63 & 64.70 & 66.42 \\
5 & 56.67 & 66.93 & 65.96 & 72.02 & 74.22 \\
6 & 56.81 & 77.17 & 78.01 & 76.80 & 80.38 \\
7 & 59.69 & 86.52 & 83.53 & 80.47 & 84.06 \\
8 & 62.80 & 94.51 & 96.43 & 86.98 & 87.12 \\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.9\textwidth]{figures/throughput_llama3_bs.pdf}
\caption{Representative end-to-end throughput on Meta-Llama-3-8B with 4096 input tokens and 256 output tokens. The compressed 70/70 + 2-bit path outperforms dense decoding in the small-to-medium batch regime.}
\label{fig:throughput}
\end{figure}

\paragraph{Compression overhead.}
Compression is useful only if its own overhead remains controlled. Table~\ref{tab:compression-summary} reports representative prefill-time compression statistics. The results show that the 2-bit sparse-quant path achieves substantially higher compression ratios than sparse-only baselines, while also reducing write-back cost enough to keep compression time competitive. For example, at batch size 8, Sparse70 reduces the KV cache to 1964.29MB, whereas Sparse70+2bit further reduces it to 1176.25MB and also lowers compression time from 5411.29ms to 4293.18ms. This indicates that the efficiency benefit of \method\ comes not only from lower decode-time bandwidth demand, but also from a more compact compression pipeline.

\begin{table}[t]
\centering
\caption{Representative compression overhead and KV-cache size for Meta-Llama-3-8B (input length 4096). Compression ratio is original KV size divided by compressed KV size.}
\label{tab:compression-summary}
\setlength{\tabcolsep}{5pt}
\resizebox{\textwidth}{!}{
\begin{tabular}{ccccc}
\toprule
Batch Size & Method & KV-Cache Size (MB) & Compression Ratio & Compression Time (ms) \\
\midrule
\multirow{4}{*}{1}
& Sparse50 & 2720.16 & 1.51$\times$ & 740.80 \\
& Sparse70 & 1964.29 & 2.09$\times$ & 716.30 \\
& Sparse50+2bit & 1261.16 & 3.25$\times$ & 545.18 \\
& Sparse70+2bit & 1176.25 & 3.48$\times$ & 552.86 \\
\midrule
\multirow{4}{*}{8}
& Sparse50 & 2720.16 & 1.51$\times$ & 5688.23 \\
& Sparse70 & 1964.29 & 2.09$\times$ & 5411.29 \\
& Sparse50+2bit & 1261.16 & 3.25$\times$ & 4317.88 \\
& Sparse70+2bit & 1176.25 & 3.48$\times$ & 4293.18 \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Component Ablation}

To understand which parts of the framework matter most, Table~\ref{tab:component-ablation} reports an ablation under the most aggressive tested configuration, namely 70/70 sparsity with 2-bit quantization. We compare the full method against three degraded variants: replacing differential sparsity with a uniform sparsity budget, removing Hadamard rotation, and replacing per-token-tile quantization with a coarser alternative. The results show that all three changes hurt performance. The largest drop comes from removing Hadamard rotation, which confirms that low-bit key quantization is highly sensitive to outliers. The gap between the full method and the uniform-sparsity variant also confirms that better budget allocation remains important even after quantization is introduced.

\begin{table}[t]
\centering
\caption{Component ablation on Meta-Llama-3-8B under 70/70 sparsity and 2-bit quantization.}
\label{tab:component-ablation}
\setlength{\tabcolsep}{5pt}
\resizebox{\textwidth}{!}{
\begin{tabular}{lccccc}
\toprule
Method & NarrativeQA & HotpotQA & GovReport & LCC & Avg. \\
\midrule
Full \method & \textbf{22.71} & \textbf{44.88} & \textbf{24.18} & \textbf{46.24} & \textbf{34.50} \\
w/o Differential Sparsity & 22.48 & 44.72 & 23.84 & 45.70 & 34.19 \\
w/o Hadamard & 22.21 & 44.16 & 22.97 & 44.61 & 33.49 \\
w/o Per-Token-Tile & 22.33 & 44.29 & 23.18 & 44.98 & 33.70 \\
\bottomrule
\end{tabular}}
\end{table}

Taken together, these results support the main thesis of \method: high-ratio KV-cache compression works best when token selection, numeric representation, and execution path are designed as one system.

% \section{Related Work}
% \label{sec:related}

% Prior work on KV-cache optimization mainly follows two directions. The first reduces the amount of retained history through token eviction or sparsification. Representative methods such as StreamingLLM~\cite{xiao2024streaming}, H$_2$O~\cite{zhang2023h2o}, SnapKV~\cite{snapkv}, and PyramidKV~\cite{pyramidkv} retain only selected historical tokens, while ThinK~\cite{xu2025think}, KVPruner~\cite{lv2024kvpruner}, and MUSTAFAR~\cite{joo2025mustafar} further explore channel-wise, structured, and unstructured sparsity. These studies show that historical tokens are not equally important and that importance-aware cache reduction can effectively relieve memory and bandwidth pressure.

% The second direction compresses the numerical representation of KV-cache elements. KVQuant~\cite{hooper2024kvquant}, QuaRot~\cite{ashkboos2024quarot}, and KIVI~\cite{liu2024kivi} explore ultra-low-bit KV representation, distribution reshaping, and asymmetric quantization for Keys and Values. These methods directly reduce KV-cache footprint and bandwidth cost, but they mainly focus on how to quantize each cache element rather than which historical information should be preserved.

% Overall, prior work shows that both sparsification and quantization are effective, but the two are still largely studied in isolation.In practice, joint compression is not a simple sequential combination: once sparsification changes the retained tokens and cache access pattern, the appropriate quantization granularity, compressed data layout, and decode-time kernel design also change. Without such co-design, algorithm-level compression gains may not reliably translate into practical end-to-end speedups. To address this gap, \method\ jointly designs differential sparsification, Per-Token-Tile quantization, and sparse-quant data layout and decode kernels to achieve a more stable accuracy--efficiency trade-off under high compression ratios.


\section{Discussion and Future Work}
\label{sec:discussion}

% 先总述一句：there are several limitations / directions for future work
% 然后只保留 3 个点
% 每个点都遵循固定句式：
% 当前做到什么 → 还没做到什么 → 留待未来
% 不展开讲太多实验现象，不重复前文分析

% Although JSQKV achieves a good balance between accuracy and efficiency in long-context scenarios, there are still three problems that need further study.

% First, the effectiveness of JSQKV comes from explicit token importance evaluation, instead of using a uniform sparsity budget. However, the current importance modeling is still mainly at the token level. It still does not describe finer-grained sensitivity across different layers, different attention heads, and different cache objects, such as Key and Value. At the same time, the quantization part has not yet been jointly designed with finer-grained sensitivity analysis and budget allocation. In the future, it is worth studying finer-grained importance modeling for layers, heads, and Key/Value, as well as matched joint quantization strategies.

% Second, the joint sparse-quant path is not better under all serving conditions. In our experiments, the clearest throughput gains mainly appear in small- and medium-batch scenarios, where the main bottleneck is still KV-cache memory access. As batch size becomes larger, the benefit brought by compression is gradually offset by dequantization overhead, metadata processing, and higher compute pressure. Therefore, how to design more efficient joint sparse-quant operators is still a key problem in this direction.

% Finally, the deployment scope of the current study is still limited. Although the experiments already cover multiple model families and representative long-context settings, they do not systematically study longer contexts, more complex interactive workloads, or a wider range of hardware platforms. In the future, it is necessary to further evaluate whether the proposed method is still effective in ultra-long-context scenarios, on heterogeneous accelerators, and in environments closer to real production deployment.

% In this paper, JSQKV achieves a favorable accuracy-efficiency trade-off in long-context scenarios. However, there are still three limitations that remain for future work.

% Firstly, the effectiveness of JSQKV benefits from explicit token-importance evaluation rather than a uniform sparsity budget. However, the current importance modeling still mainly operates at the token level and does not yet capture finer-grained sensitivity across different layers, attention heads, and cache objects such as Keys and Values. In addition, the quantization component has not yet been jointly designed with finer-grained sensitivity analysis and budget allocation. Future work could therefore explore more fine-grained importance modeling for layers, heads, and Key/Value representations, together with better matched joint quantization strategies.

% Secondly, the joint sparse-quant path does not remain advantageous under all serving conditions. In our experiments, the most significant throughput gains mainly appear in small- to medium-batch scenarios, where KV-cache memory access is still the dominant bottleneck. As batch size increases, however, the benefit brought by compression is gradually offset by dequantization overhead, metadata handling, and increased compute pressure. Therefore, designing more efficient joint sparse-quant operators remains an important direction for future research.

% Finally, the current study is still limited in deployment scope. Although the experiments cover multiple model families and representative long-context settings, they do not yet systematically evaluate longer contexts, more complex interactive workloads, or a broader range of hardware platforms. Future work is needed to further examine whether the proposed approach remains effective in ultra-long-context scenarios, on heterogeneous accelerators, and in environments closer to real-world production deployment.

Although JSQKV achieves a favorable accuracy-efficiency trade-off in long-context scenarios, several limitations remain for future work. 

Firstly, its effectiveness relies on explicit token-importance evaluation rather than a uniform sparsity budget, yet the current design still mainly operates at the token level and does not capture finer-grained sensitivity across layers, attention heads, or Key/Value representations. A more fine-grained joint design of importance modeling and quantization is therefore worth exploring. Secondly, the joint sparse-quant path is not advantageous under all serving conditions. Its largest throughput gains appear in small- to medium-batch scenarios, while at larger batch sizes the benefit of compression is increasingly offset by dequantization, metadata handling, and higher compute pressure. Designing more efficient sparse-quant operators thus remains an important direction. Finally, the current evaluation scope is still limited. Although experiments cover multiple model families and representative long-context settings, further study is needed to verify whether the proposed method remains effective in ultra-long-context scenarios, on heterogeneous accelerators, and in production-like deployment environments.

\section{Conclusion}
\label{sec:conclusion}

\method\ reduces KV-cache cost through differential sparsification, stabilized low-bit quantization, and a bitmap-based sparse-quant decode kernel. Evaluation results show consistent gains over a sequential sparsity-plus-quantization baseline under matched compression budgets, and they further show that compression savings can translate into practical throughput gains during decoding. Overall, the results support treating KV-cache compression as a systems-and-algorithms co-design problem rather than as a simple composition of independent post-processing steps.

\bibliographystyle{splncs04}
\bibliography{refs}

\end{document}
